# 智慧绩效系统 - 考核与强制分布控制 PRD

## 1. 产品概述

### 1.1 背景与目的
在企业绩效考核过程中，为了保证评价的公平性和结果的合理分布，通常需要引入“强制分布”机制与“绩效等级匹配”规则。本功能模块旨在提供灵活的考核等级生成机制与强制分布管控能力，并在流程的任意审核节点施加控制，以满足不同企业的差异化管理诉求。

### 1.2 核心价值
- **灵活的等级生成规则**：支持“按分数区间生成”与“按分数排名生成”两种核心匹配模式，适应不同考核场景。
- **细颗粒度的节点管控**：可在“考核结果设置（模拟）”中针对流程中的不同节点（如直接上级审核、HR调整等）独立开启分布规则管控。
- **多维度分组控制**：针对部门层级、考核组、自定义分组等不同维度独立下发控制比例/人数，应对复杂组织架构与业务需要。

---

## 2. 功能范围与架构

### 2.1 功能模块划分
1. **绩效等级配置（基础设置）**
   - 定义等级规则（如 S/A/B/C/D）
   - 配置分数匹配规则（按分数区间、按分数排名）
2. **强制分布规则库（模板中心）**
   - 规则列表页（查看、编辑、新建、删除）
   - 规则明细配置（按比例/按人数控制，及特定算法处理）
3. **考核结果设置/模拟（业务应用层）**
   - 基础设置（等级规则引用与全局限制）
   - 强制分布控制（各审核节点管控参数、分组独立参数配置）

### 2.2 页面层级关系
- 一级导航：设置 -> 考核设置 -> 绩效等级
- 一级导航：设置 -> 考核设置 -> 强制分布规则
- 一级导航：考核管理 -> 员工考核 -> 考核结果设置模拟页

---

## 3. 详细功能说明

### 3.1 绩效等级与强制分布规则配置
**业务逻辑**：
- **绩效等级匹配规则：** 支持配置为“按分数区间”或“按分数排名”。支持设定统一的分数下限、上限与预期名次比例。当按分数排名时，还可针对同分超额情况进行处理。
- **强制分布规则模板：** 提供独立的模板管理中心，规则可根据比例或数量进行定义，并在考核方案或结果设置中被直接引用拉取。

### 3.2 考核结果设置模拟页
**入口**：考核管理 -> 员工考核 -> 考核结果设置模拟

**功能描述**：
#### 3.2.1 基本设置
- **绩效等级规则**：核心分流开关，提供选项 `[不开启, 按分数区间生成, 按分数排名生成]`。
- **等级规则（联动）**：选择后展示下拉选中的对应等级体系（如 4.29）。
- **等级排名控制范围（特有逻辑）**：**仅当**绩效等级规则选择 **「按分数排名生成」** 时展示。
  - 下拉框：提供选项 `[全员, 按部门, 按考核组, 按自定义分组]`。
  - 特殊下钻：当选择 `按部门` 时，右侧联动展示【部门层级】配置（选项包括 `最末级, 1级部门, 2级部门`），默认选中最末级。以此定义排名的对比池。

#### 3.2.2 强制分布控制 (基于节点的流程管控)
通过平级 Tab 切换展示各个审批节点（如 HR调整、直接上级审核、部门负责人审核）。针对每一个打开“是否执行强控”功能的节点：

1. **控制方式**
   - 单选：`[仅提醒, 阻止提交]`。决定当提交审核不符合强制分布指标时，系统的阻断力度。
2. **控制范围（特有逻辑）**
   - **仅当**最上方绩效等级规则选择 **「按分数区间生成」** 时展示。
   - 单选：`[全部参与人员, 绩效组]`。
   - 若选择 `绩效组`，则唤起范围维度的下拉选择框 `[按部门, 按考核组, 按自定义分组]`。
   - 同样，若在此处选择 `按部门`，右侧联动展示【部门层级】配置（最末级、1级部门等）。
3. **控制分组视图**
   - **全员/统一视图**：若全局/节点层面的范围配置为“全员”或“全部参与人员”，下方直接展示当前节点的「控制规则参数」与「强制分布预测表」。
   - **左树右表多组视图**：若范围配置使用了任何分组依据（按部门/按考核组/按自定义分组），界面下方将通过 **「左侧：绩效组列表 + 右侧：详情配置卡片」** 的结构进行布局。
   - 用户可以点击左侧列表的各个具体分组（如“产品部”、“研发部”等），在右侧单独给对应分组指定**强制分布规则**及细节处理。
4. **控制规则及参数配置**
   - **强制分布规则**：当处于多组视图下，允许下拉选择具体的规则模板源（默认为考核方案配置的规则带入）。
   - **控制规则**：下拉选择 `[按等级分布比例, 按等级分布人数]`。
   - **防冲突算法**：
     - 若为“按比例”，展示【余数处理】选择方案 `[迁移到下一等级, 迁移到指定等级]`。
     - 若为“按分布人数”，展示【同分超人数规则】选择方案 `[均保持当前等级, 均降到下一等级]`。
5. **人数预测表格**
   - 处于节点的管控区域最下方，基于左侧所选区域的总参评人数及右侧下发的控制比例/人数，实时向 HR 反馈预计算的各个级别最终落定人数（即 Expected Number），起到所见即所得的确认作用。

---

## 4. UI/UX 规范约束

1. **统一视效**：主色调遵循 `#15B8A6`，警示/阻断采用强对比状态展示。基础背景 `#F9FAFB`。
2. **沉浸式表单**：页面采用白色背景卡片承载，利用 8px 圆角与微弱阴影包边。所有左树右表的局部切换容器，必须在视觉上提供清晰的激活态标识（如左侧高亮背景与左侧边框边条指示）。
3. **空间排布**：单行表单配置统一通过左对齐 Label(宽 120px) + 右侧操作元件的形式进行排布。
4. **实时响应**：改变顶部「按分数排名」还是「按分数区间」时，节点的「控制范围」项与「分组视图」应当具备平滑过渡显隐特性，不产生断层闪烁。

## 5. 附录
- 本文档持续迭代，后续视复杂部门维系或特殊层级追溯时逐步增加补充用例。
